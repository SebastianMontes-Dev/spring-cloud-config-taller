# Copiar a .env y completar. .env esta en .gitignore: nunca subir credenciales reales.
MYSQL_ROOT_PASSWORD=cambiar_esto
MYSQL_PASSWORD=cambiar_esto
# Opcional (por defecto 3308; el 3306 suele estar ocupado por un MySQL local)
# MYSQL_HOST_PORT=3308
